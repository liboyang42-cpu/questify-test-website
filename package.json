{
  "name": "atlab-inception-rebuild",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:demos": "INCLUDE_DEMOS=1 vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5186 --strictPort",
    "lint": "node lint.mjs",
    "smoke": "node --test tests/",
    "smoke:selftest": "node tests/selftest.mjs",
    "ci": "npm run lint && npm run build && npm run smoke"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "vite": "^5.4.0"
  },
  "packageManager": "npm@10.9.7",
  "engines": {
    "node": ">=20"
  }
}
